import { _decorator, Component, Node } from 'cc';
import { UIComp } from '../../framework/ui/UIComp';
const { ccclass, property } = _decorator;

@ccclass('ChatIR')
export class ChatIR extends UIComp {
    protected dchg(): void {
        
    }
}

